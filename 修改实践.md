# 修改实践：基于 MimicLite 的 PND Adam Lite 跳跃训练

本文把《技术方案 v1.0》拆成可单独验收的阶段，作为实施顺序和闸门清单。  
当前只规划，不默认开训。每个阶段通过验收后再进入下一阶段。

## 文档关系

- 当前方案：`技术方案.md`（有效）；开训前规划：`技术方案v1.0.md`
- 训练框架：MimicLite（`active-adaptation/projects/mimic-lite/`）
- 仅借用资源：`pnd_jump-main` 的 12DOF 机器人和跳跃运动数据
- 不使用：`pnd_jump` 的任何训练代码

## 先对齐的实现约束

### 机器人必须是 Adam Lite，不是 G1

训练对象固定为 **PND Adam Lite 原生 12DOF 模型**，不做 G1 替换、不做 G1→Adam 关节映射。

- 使用模型：`pnd_jump-main/resources/robots/adam_lite/adam_lite_12dof.xml`
- 12 个驱动关节：每条腿 `hipPitch / hipRoll / hipYaw / kneePitch / anklePitch / ankleRoll`
- 浮动基座：`pelvis`
- 不加载 G1 MJCF，不复用 G1 body / joint 名称，不使用 G1 运动数据来训 Adam Lite

文档里提到 G1 / Atom，**只表示 MimicLite 的配置与资产注册写法**（Hydra 怎么组、`assets/*.py` 怎么写），不是用那台机器人。

### MimicLite 接口约束

v1.0 中的 YAML 是示意稿。MimicLite 的真实结构如下，后续改动必须按真实框架落地，而不是按示意键名硬写：

- 任务配置走 Hydra 组合，参考 `tracking-base.yaml`、`tracking-base-atom.yaml` 的**文件结构**
- 运动数据是 any4hdmi 的 `qpos` NPZ + `manifest.json`，不是直接读取 BVH JSON
- 资产注册参考 `mimic_lite/assets/g1.py`、`mimic_lite/assets/atom.py` 的**代码模式**，内容改为 Adam Lite
- 命令是 `mimic_lite.RobotTracking`；动作层在残差控制下接到参考轨迹，而不是换成 G1 默认动作空间

跳跃 JSON 已从 Hugging Face `pndbotics/adam_lite_dataset` 下载到本仓库 `adam_lite_dataset/`。GitHub `pnd_jump` 本身不含这份数据。

## 总览

| 阶段 | 目标 | 是否训练 | 建议闸门 |
|------|------|----------|----------|
| 0 | 对齐决策、确认缺失资源 | 否 | **已锁定**（见下） |
| 1 | 接入 12DOF 机器人 | 否 | **已通过**（CPU 冒烟） |
| 2 | 跳跃数据转 MimicLite 格式 | 否 | **已通过**（CPU FK：高度抬升 + 双脚离地） |
| 3 | 任务 / 奖励 / 控制配置 | 否 | **已通过**（Hydra + GPU env reset） |
| 4 | 开环冒烟（零策略） | 否 | **已通过**（`openloop_cannot_track`，无 NaN） |
| 5 | PPO 小规模验证 | 是（单卡） | **训练已跑完**：无 NaN，存活/回报上升；完整跳跃形态未达到 |
| 6 | 全量 PPO 或 PPO-ROA | 是 | **进行中**（单卡 PPO-ROA，`train → adapt → finetune`） |
| 7 | 评估导出 | 否 | **已通过**（高度/起飞/腾空/成功率） |
| 落地/存活/sim2sim | 矫正 GRF、冲击惩罚、跑完 clip、pnd MuJoCo | 短 finetune | **后处理已冻结 ckpt_1000 + deploy 踝 PD** |

建议执行顺序：阶段 3–4 已在 GPU 上通过。下一阶段是阶段 5 单卡 PPO。

---

## 阶段 0：对齐决策（已锁定）

### 目的

避免按示意 YAML 硬接，和 MimicLite 实际接口打架；同时锁定训练机器人是 Adam Lite，不是 G1。

### 已锁定决策

| 项 | 选择 | 含义 |
|----|------|------|
| 机器人 | **原生 Adam Lite 12DOF** | 用 `adam_lite_12dof.xml` 训练跳跃。G1 / Atom 只作 MimicLite 写法参考，不参与仿真与训练。 |
| 控制 | **残差控制** | `q_des = q_ref + action * 0.35`，与 pnd_jump 一致。 |
| 观测 | **MimicLite 原生观测** | 训练不裁成 pnd_jump 的 82 维；评估仍对标跳跃高度、腾空、成功率。 |
| 奖励 | **MimicLite 奖励骨架 + pnd_jump 权重映射** | 不重写一套奖励；缺的项（如残差 L2）再补。 |

### 明确不做

- 不用 G1 模型、G1 关节名、G1 运动集来训 Adam Lite
- 不做 G1→Adam Lite 关节映射
- 不把 pnd_jump 训练代码迁进 MimicLite

### 闸门

阶段 0 已通过。阶段 1 已完成。

---

## 阶段 1：机器人模型接入（已完成）

### 做什么

- 从 `pnd_jump-main/resources/robots/adam_lite/` 复制 **Adam Lite** 的 `adam_lite_12dof.xml` 和 mesh（不是 G1 XML）
- 按 `g1.py` 的**注册写法**新建 `mimic_lite/assets/adam_lite/` 包，内容全部是 Adam Lite 的 body / joint / 执行器
- 在 `mimic_lite/assets/__init__.py` 注册 `adam_lite`，不改 G1 资产
- 配置 12 关节 PD 增益：

| 关节 | stiffness | damping |
|------|-----------|---------|
| hipPitch | 305 | 6.1 |
| hipRoll | 700 | 30 |
| hipYaw | 405 | 6.1 |
| kneePitch | 305 | 6.1 |
| anklePitch | 25 | 3 |
| ankleRoll | 0（mjlab 执行器改为 1.0） | 0.35 |

- 默认站立姿态：`[-0.32, 0.0, -0.18, 0.66, -0.29, 0.0, -0.32, 0.0, 0.18, 0.66, -0.29, 0.0]`

### 实际落地时的调整

- 资产定义放在 `mimic_lite/assets/adam_lite/__init__.py`，避免和模型目录同名冲突
- 当前 MuJoCo 3.11 无法直接编译原版视觉 OBJ（网格体积过小，设 `inertia=shell` 又会被系统杀掉）。阶段 1 使用**碰撞体物理模型**：腰/臂 equality 固定保留，视觉 mesh 先去掉。回放外观可以在阶段 4 再加回来
- 本机 venv 原先指向 `/root/autodl-tmp/mimic-repro/`。已把 `mimic_lite` 可编辑安装改到当前仓库 `mimic-repro-adam-lite`

### 交付

- `projects/mimic-lite/mimic_lite/assets/adam_lite/__init__.py`
- `projects/mimic-lite/mimic_lite/assets/adam_lite/adam_lite_12dof.xml`
- `projects/mimic-lite/mimic_lite/assets/adam_lite/assets/*.obj`（保留原 mesh，当前 XML 未引用）
- `projects/mimic-lite/scripts/check_adam_lite_asset.py`
- 资产注册修改：`projects/mimic-lite/mimic_lite/assets/__init__.py`

### 验收（已通过，CPU，不需要训练卡）

- 注册名：`adam_lite_12dof`
- MJCF 可编译：`nq=32`，12 个驱动关节名称与 XML 一致
- 默认站姿：pelvis z=0.92，`hipPitch=-0.32`，`kneePitch=0.66`
- mjlab 包装：6 组 PD 执行器覆盖 12 腿关节，2 个接触传感器

### 不包含

运动数据、任务配置、训练。

---

## 阶段 2：跳跃数据集转换（已完成）

### 做什么

- 定位或补齐 `0005_2FeetJump001.bvh.json`
- 转成 any4hdmi 格式：`manifest.json` + `motions/*.npz`（`qpos[T, nq]`）
- 关节顺序与 12DOF MJCF 的 `qpos` 对齐
- 写 `cfg/task/motion/adam_lite/jump.yaml`

转换时可复用 `pnd_jump-main/legged_gym/utils/motion_loader.py` 的读取逻辑，但输出必须是 MimicLite / any4hdmi 的 NPZ，而不是 pnd_jump 训练侧格式。

### 实际落地

- 源数据：`adam_lite_dataset/adam_lite/sfu/0005_2FeetJump001.bvh.json`（120 Hz，2565 帧，约 21.4 s）
- JSON 根姿态四元数是 **xyzw**，写入 MuJoCo qpos 时转为 **wxyz**
- `qpos` 保持源帧率，不在转换时重采样到 0.02；MimicLite 加载时按 `target_fps=50` 插值
- 12 个腿关节从 JSON 填入；腰/臂按 12DOF equality 锁定位姿（肘 `-1.6`，其余 `0`）。JSON 没有腕关节通道
- 输出目录按 MimicLite 惯例放在 `any4hdmi/output/adam_lite/jump/`，而不是示意路径 `output/adam_lite_jump/`

### 交付

- `projects/mimic-lite/scripts/convert_bvh_to_mimiclite.py`
- `any4hdmi/output/adam_lite/jump/manifest.json`
- `any4hdmi/output/adam_lite/jump/motions/0005_2FeetJump001.npz`
- `projects/mimic-lite/cfg/task/motion/adam_lite/jump.yaml`

### 验收（已通过，CPU，不需要训练卡）

- `qpos_dim=32`，与模型 `nq` 一致
- FK：pelvis 从 0.93 m 抬到 1.23 m（相对起始高度 +0.29 m）
- 足底碰撞盒：最长双脚离地 0.44 s，足底最高约 0.24 m；站立/下蹲阶段足底贴近地面

### 不包含

任务 YAML、开环 play、训练。

---

## 阶段 3：任务配置（已完成）

### 做什么

跟 Atom 的 `tracking-base-atom.yaml` 同结构，不按 v1.0 示意键名硬写。

需要落地的配置：

- 新建 `tracking-adam_lite-jump.yaml`
- body / joint 名改成 Adam Lite：`pelvis`、thigh / shin / toe、12 腿关节
- 残差控制 + `action_scale=0.35`
- RSI 映射到 MimicLite 的 reset / rewind（对齐 pnd_jump 的 `rsi_margin_frames=50`、phase-balanced）
- 终止：pelvis 碰撞、倾倒、相对参考高度过低
- 域随机：摩擦 `[0.4, 1.6]`，质量扰动，`push_robots` 先关

### 实际落地

- 任务 YAML 按 Atom/G1 的 Hydra 组合写，机器人名 `adam_lite_12dof`
- 残差在 `mimic_lite.JointPosition(residual=true)`：`q_des = q_ref + action * 0.35`。G1/Atom 默认仍是 `default + action * scale`
- RSI：`command.py` 增加可选 `rsi` / `rsi_margin_frames=50` / `rsi_mode=phase_balanced`，默认关闭，不影响 G1
- 奖励用 MimicLite 模块，权重按 pnd_jump 映射；补了 `residual_l2`
- 终止：`cum_body_pos_error`、`cum_body_z_error(0.35)`、`fall_over(xy_thres=0.82)`
- 摩擦 `[0.4, 1.6]`，pelvis 质量 `[0.95, 1.05]`，未加 `perturb_root_vel`（push 关闭）
- 跳跃数据路径写成绝对路径，因为当前 venv 的 `active_adaptation` 仍来自隔壁 `mimic-repro`

### 交付

- `projects/mimic-lite/cfg/task/tracking-adam_lite-jump.yaml`
- `projects/mimic-lite/cfg/task/termination/adam_lite_jump.yaml`
- `projects/mimic-lite/cfg/task/motion/adam_lite/jump.yaml`
- `projects/mimic-lite/scripts/check_adam_lite_jump_task.py`
- 小改：`mimic_lite/tasks/command.py`（RSI）、`mimic_lite/tasks/actions.py`（残差）、`mimic_lite/tasks/rewards/common.py`（residual_l2）

### 验收（已通过，GPU）

- Hydra compose `task=tracking-adam_lite-jump`：残差、0.35、phase-balanced RSI、12 关节、跳跃 NPZ 路径齐全
- GPU env reset：`num_envs=4`，`motion_len=1069`，`ref_joint_pos` 末维 12，`t=1`
- `aa.init()` 会从隔壁 `mimic-repro` 的 `.cache/projects.json` 再 import `mimic_lite`，必须指向本仓库
- mjlab 的 position actuator 不能 `stiffness=0`（`effort/stiffness` 除零）。ankleRoll 改为 **Kp=1.0**，仍远弱于其他关节
- any4hdmi FK cache 在 CUDA 初始化后用 DataLoader fork 会卡死；脚本里固定 `ANY4HDMI_CACHE_BUILD_NUM_WORKERS=0`、CPU FK、缓存根目录在本仓库 `.cache/motion`

### 不包含

真训练。

---

## 阶段 4：开环冒烟（已完成）

### 做什么

- 用 `scripts/openloop_adam_lite_jump.py`（Hydra `play.yaml`）开 4 个环境
- 策略前向后把 `action` 置零，只看残差 PD 跟踪 `q_ref`
- 记录：根高度、存活、是否 NaN

### 实际结果（GPU，4090 D）

- 无 NaN，环境可 reset / step
- 分类：**`openloop_cannot_track`**
  - 平均存活 1.11 s，2 s 内全部终止（`instant_crash_frac=1.0`）
  - 起始高度 0.933 m 对齐参考 0.935 m
  - 开环峰值抬升 0.11 m，参考峰值 0.23 m
- 结论：纯 PD 跟不上跳跃，必须靠 RL。这仍算阶段 4 通过（闸门是不秒崩、能分类，不是开环就要跳好）

记录：`active-adaptation/openloop_adam_lite_jump.json`

### 为什么这一步不能省

落地冲击是 v1.0 里标成高概率的风险。开环已暴露：残差 PD 在 1 s 左右就会被终止条件打断，阶段 5 的 PPO 需要把跟踪误差压下去。

---

## 阶段 5：PPO 单阶段小规模训练（已完成）

### 做什么

- 单卡
- `task=tracking-adam_lite-jump`
- `+algo/ppo/module=base`（`train.yaml` 默认列表里没有 `algo/ppo/module`，不能写成 `algo/ppo/module=base`）
- `task.num_envs=1024`
- `total_frames=10_000_000`
- `wandb.mode=disabled`

参考命令：

```bash
export OMP_NUM_THREADS=8 MKL_NUM_THREADS=8 MUJOCO_GL=disabled PYTHONUNBUFFERED=1
cd active-adaptation
./venv/mjlab/.venv/bin/python projects/mimic-lite/scripts/train.py \
  task=tracking-adam_lite-jump \
  +algo/ppo/module=base \
  task.num_envs=1024 \
  total_frames=10_000_000 \
  wandb.mode=disabled
```

### 实际结果（GPU，4090 D，约 13 分钟，305 iter）

- 无 NaN（`env/nonfinite_rows_total=0`），训练正常结束
- 存活：`episode_len` 21.7 → 77.6 step（0.43 s → 1.55 s）；开环是 1.11 s
- tracking return：120 → 438
- `fall_over` 0.41 → 0.12；`root_z_below_ref` 0.65 → 0.10
- 仍远短于 clip 长度（1069 帧 / 21 s），10M 帧还看不到完整起跳–腾空–落地

输出：

- `active-adaptation/outputs/2026-09-08/11-59-04-AdamLiteJumpTrack-mimic_lite_ppo/`
- `checkpoint_305.pt`、`metrics.jsonl`

### 验收（放行全量的最低线）

- 训练能跑完、不 NaN：**已通过**
- 相对开环：存活变长、跟踪回报上升：**已通过**
- 出现可辨认的起跳–腾空–落地： **未达到**（平均只活约 1.6 s）

### 不包含

8 卡、ROA 三阶段。

---

## 阶段 6：全量训练

阶段 5 过了再开。优先路径 B。本机只有 1 张 4090 D，不能按文档的 8 卡 / `nproc_per_node=8` / `num_envs=8192` 原样跑。

### 路径 B：PPO-ROA 三阶段（已完成）

`train_sequential.py` 一次拉起 `train → adapt → finetune`，不要拆成三次手写 `stage=train`。

单卡命令：

```bash
export OMP_NUM_THREADS=8 MKL_NUM_THREADS=8 MUJOCO_GL=disabled PYTHONUNBUFFERED=1
cd active-adaptation
./venv/mjlab/.venv/bin/python projects/mimic-lite/scripts/train_sequential.py \
  task=tracking-adam_lite-jump \
  stages=adam_lite_jump \
  +task/patches=teacher_future_t16 \
  algo/ppo_roa/module=huge \
  task.num_envs=4096 \
  nproc_per_node=1 \
  wandb.mode=disabled \
  checkpoint_interval=500 \
  upload_interval=500
```

run：`active-adaptation/outputs/2026-09-08/12-21-37-train-sequential/`，约 4.3 h，exit 0。

| 阶段 | iter | 耗时 | 末轮 `root_pos` / `joint_pos` reward |
|------|------|------|--------------------------------------|
| `01-train` | 4000 | ~3.4 h | 3.24 / 1.94 |
| `02-adapt` | 500 | ~7 min | 1.85 / 1.78 |
| `03-finetune` | 1000 | ~42 min，含 `perturb_root_vel` | 2.00 / 1.87 |

- VRAM 约 9.7 / 24 GB，无 NaN
- 最终权重：`stages/03-finetune/checkpoint_latest.pt` → `checkpoint_1000.pt`

路径 A（直接大 PPO / 8 卡）未跑。

---

## 阶段 7：评估与导出

固定 clip、`start_from_zero`，64 并行环境，finetune `checkpoint_1000.pt`。

```bash
./venv/mjlab/.venv/bin/python projects/mimic-lite/scripts/eval_adam_lite_jump.py \
  task=tracking-adam_lite-jump \
  +task/patches=teacher_future_t16 \
  +exp=ppo_roa/finetune \
  algo/ppo_roa/module=huge \
  checkpoint_path=.../stages/03-finetune/checkpoint_latest.pt \
  headless=true \
  task.num_envs=64 \
  task.command.start_from_zero=true \
  export_policy=true
```

### 结果（无 NaN）

| 指标 | 目标 | 实测 | 判定 |
|------|------|------|------|
| 跳跃高度误差 | < 0.05 m | **0.032 m**（81% 环境过线） | 通过 |
| 起跳速度峰值 | > 2.0 m/s | **2.67 m/s**（100%） | 通过 |
| 腾空时间 | > 0.3 s | **0.45 s**（100%） | 通过 |
| 跳跃成功率 | > 95% | **98.4%** | 通过 |
| 平均存活 | clip ~21.4 s | **18.5 s** | — |
| 峰值高度 | ref 0.29 m | 0.26 m | — |
| 落地冲击力 | < 500 N（观察） | 4318 N | 观察项，未卡死 |
| 能量效率 | < 50 J/cm（观察） | 未算 | 观察项 |

回放过程中 `z` 能跟上参考轨迹的起跳–落地形状（2.7 s 时 z≈1.18 vs ref 1.23）。

### 交付

- 评估 JSON：`active-adaptation/outputs/2026-09-08/12-21-37-train-sequential/eval_adam_lite_jump.json`
- ONNX：`projects/mimic-lite/scripts/exports/AdamLiteJumpTrack/policy-wlvs4cnd-unknown.onnx`
- 部署 YAML：同目录 `policy-wlvs4cnd-unknown.yaml`

`pnd_jump` MuJoCo deploy 对比尚未做。

### 约束

对比部署可以借用 `pnd_jump` 的仿真/真机部署脚本，但仍然不碰它的训练代码。

---

## 落地冲击 / 整段存活 / sim2sim（进行中）

阶段 7 过了之后，先把三件事摸清再改，而不是按 v1.0 的 500 N 硬卡。

### 落地力读数为什么是 4318 N

`eval_adam_lite_jump.py` 原先写的是：

`force_history[:, feet].norm().amax(history).sum(two feet)`

mjlab 接触传感器是 `[N, B, H, 3]`，`history_length=4`。这会把 **4 步历史里的峰值再对双脚求和**，不是同一时刻的总 GRF。4318 N 被夸大了。

机器人整机大约 55 kg，站立总 GRF 已经约 540 N。v1.0 的「峰值 < 500 N」大约是 1 倍体质量，落地峰值达不到。评估改为：

- 只用历史最后一格（当前步）
- `peak_total_grf_n`：双脚同时刻求和后再对时间取 max
- `peak_per_foot_n`：单脚峰值

500 N 只作观察，不再当闸门。

### 奖励 / 终止

原先 `loco` 没有接触力惩罚。PPO-ROA critic 按 **reward group** 计头，在 `loco` 里加一项不会改 critic 维度，可以从 `checkpoint_1000.pt` 接着 finetune。

改动：

| 项 | 旧 | 新 |
|----|----|----|
| `feet_force_excess` | 无 | 单脚超 500 N 软惩罚，weight 0.6 |
| `root_pos_error` 终止 | 0.40 m（timeout） | 0.55 m |
| `survival` | 0.15 | 0.25 |
| `root_linvel` | 1.2 | 1.5 |
| `action_rate_l2` | 0.01 | 0.02 |
| `residual_l2` | 0.05 | 0.08 |

平均存活 18.5 s / clip 21.4 s，主要是 `root_pos_error` 把行进跳跃提前判超时。放松阈值，同时加一点存活和速度跟踪。

### pnd_jump 部署入口（只借场景和 PD 环）

- 脚本：`pnd_jump-main/deploy/deploy_mujoco/deploy_mujoco_jump.py`
- 配置：`deploy/deploy_mujoco/configs/adam_lite_jump_sfu.yaml`（82 维 LSTM，future +5/+10）
- 场景：`resources/robots/adam_lite/scene_12dof.xml`，`dt=0.0025`，decimation=8 → 50 Hz

**不能**把 MimicLite ONNX 丢进他们的 `build_jump_obs`。导出学生网络输入是 `command=168` + `policy=246`，VecNorm 在 ONNX 里。正确 sim2sim：他们的 XML + PD 环，MimicLite 观测 / ONNX。PD 默认用训练增益（anklePitch 25 / ankleRoll 1.0），可选他们的 deploy 增益（30 / 3）做压力测试。

新增脚本：`projects/mimic-lite/scripts/sim2sim_adam_lite_jump.py`。等这次短 finetune 评估完再跑。

### 短 finetune

从 `stages/03-finetune/checkpoint_1000.pt` 再跑 600 iter（`total_iters=1600`，因为 ckpt 里记着 last_iter≈1000；直接写 600 会空转）。仍是 `+exp=ppo_roa/finetune` + `perturb_root_vel`，`freeze_vecnorm=true`。

Run：`active-adaptation/outputs/2026-09-08/17-20-52-AdamLiteJumpTrack-ppo_roa/`，最终 `checkpoint_1600.pt`，无 NaN。

### 重评估（64 env，矫正后的 GRF）

| 指标 | 旧 ckpt_1000 | 落地 FT ckpt_1600 |
|------|----------------|-------------------|
| 高度误差 | 0.032 m | **0.057 m**（闸门未过） |
| 起飞 vz | 2.67 | 2.72 |
| 腾空 | 0.45 s | 0.44 s |
| 成功率 | 98.4% | 98.4% |
| 平均存活 | 18.5 s | **16.0 s** |
| 整段 clip | — | 23% |
| 峰值总 GRF（当前步） | 当时被夸大成 4318 | **3142 N** |
| 单脚峰值 | — | 2833 N |

落地惩罚把跟踪和存活拉下来了，冲击仍在约 5–6 倍体质量，不是 500 N。旧策略若还要跳高，应继续用 `checkpoint_1000`；这轮 FT 不宜当更好的跳跃权重。

### sim2sim

脚本：`projects/mimic-lite/scripts/sim2sim_adam_lite_jump.py`。用训练 MJCF（无视觉网格）+ mjlab 同款 **position 执行器**，观测仍是 MimicLite ONNX（`command=168` + `policy=246`），不是 pnd 的 82 维 LSTM。

先前 6 s 倒地（`final_z≈0.11 m`）不是策略坏了，是部署环和训练不一致。对齐后 `checkpoint_1000` / `checkpoint_1600` 都能跑完整段 clip（~21.3 s），没有倒地。

| 缺口 | 旧 sim2sim | 训练 / 现 sim2sim |
|------|------------|-------------------|
| 物理步 | `0.0025 × 8` | `0.005 × 4` |
| 执行器 | 电机 + Python PD，扭矩 ×0.85 | MuJoCo `position`，满 effort |
| 关节 | XML 默认 armature/摩擦≈0 | `armature=0.01`，`frictionloss=0.01` |
| 积分器 | 默认 Euler | `implicitfast` |
| 起始帧 | 0 | `start_from_zero` 的 t=1 |
| 历史 | reset 后再 push 一次 | 第一步不重复写入 |
| 动作延迟 | 无 | 每个控制周期第 1 个物理步用上一拍目标 |

ckpt_1000 原生结果：存活 21.34 s，起飞 vz 2.47 m/s，腾空 0.44 s，峰值抬升 0.22 m，`final_z=0.87 m`。落地 FT（1600）同样不倒，跳高略低（0.19 m），接触力更低。

### 后处理（当前最佳）

冻结 **`checkpoint_1000`**，不要用落地 FT `checkpoint_1600`。原生部署再加 pnd 同款 **踝关节略硬 PD**（`anklePitch 30/3`，`ankleRoll 3/0.35`）：跳高几乎不变，峰值总 GRF 5348 N → **4697 N**。

| 产物 | 路径 |
|------|------|
| 一键 sim2sim | `bash sim2sim_jump_best.sh`（默认 `--pd deploy`） |
| ONNX | `.../exports/AdamLiteJumpTrack/policy-adam_lite_jump_best.onnx` |
| ckpt 软链 | `.../exports/AdamLiteJumpTrack/checkpoint_best.pt` → `checkpoint_1000.pt` |
| 摘要 | `adam_lite_jump_best.json` |
| 视频 | `adam_lite_jump_best.mp4`（对齐后的原生 MuJoCo，ckpt_1000） |

覆盖 `sim2sim_jump_best.sh` 默认时可以再传 `--pd train`。

### 硬 sim2sim（不训练）

`--mode`：`train`（H0，默认）/ `motor`（H1）/ `realish`（H2，未作为当前推荐）。

H1 已跑通（`bash sim2sim_jump_best.sh --mode motor`）：电机 PD、`0.0025×8`、扭矩 ×0.85、保留 armature、部署踝。ckpt_1000 **未倒地**，存活 21.34 s，起飞 2.51 m/s，腾空 0.44 s，抬升 0.22 m，峰值总 GRF 4693 N。JSON：`adam_lite_jump_sim2sim_h1.json`。

视频：mjlab `adam_lite_jump.mp4`；原生对齐 `adam_lite_jump_sim2sim.mp4`；后处理命名 `adam_lite_jump_best.mp4`。

---

## 代码修改清单（按阶段落地）

### 新增文件

| 文件路径 | 对应阶段 | 说明 |
|----------|----------|------|
| `projects/mimic-lite/mimic_lite/assets/adam_lite/__init__.py` | 1 | 12DOF Adam Lite 资产定义 |
| `projects/mimic-lite/mimic_lite/assets/adam_lite/adam_lite_12dof.xml` | 1 | 机器人 MJCF 模型 |
| `projects/mimic-lite/mimic_lite/assets/adam_lite/assets/*.obj` | 1 | 机器人网格文件 |
| `projects/mimic-lite/scripts/convert_bvh_to_mimiclite.py` | 2 | BVH JSON 转 MimicLite / any4hdmi 格式 |
| `projects/mimic-lite/cfg/task/motion/adam_lite/jump.yaml` | 2 | 跳跃运动配置 |
| `projects/mimic-lite/cfg/task/tracking-adam_lite-jump.yaml` | 3 | 跳跃跟踪任务配置 |
| `projects/mimic-lite/cfg/task/termination/adam_lite_jump.yaml` | 3 | 跳跃终止：高度、倾倒 |
| `projects/mimic-lite/scripts/check_adam_lite_jump_task.py` | 3 | Hydra 任务冒烟 |
| `projects/mimic-lite/scripts/openloop_adam_lite_jump.py` | 4 | action=0 开环 |
| `projects/mimic-lite/scripts/eval_adam_lite_jump.py` | 7 | 跳跃指标评估 + ONNX |
| `projects/mimic-lite/scripts/record_adam_lite_jump.py` | 7 | EGL 录视频 |
| `projects/mimic-lite/scripts/sim2sim_adam_lite_jump.py` | 落地/sim2sim | 训练 MJCF + position 执行器 + MimicLite ONNX |
| `sim2sim_jump_best.sh` | 后处理 | 一键跑 ckpt_1000 最佳部署（deploy 踝 PD） |

### 修改文件

| 文件路径 | 对应阶段 | 修改内容 |
|----------|----------|----------|
| `projects/mimic-lite/mimic_lite/assets/__init__.py` | 1 | 注册 Adam Lite 资产 |
| `projects/mimic-lite/mimic_lite/tasks/command.py` | 3 | 可选 RSI（margin=50，phase-balanced） |
| `projects/mimic-lite/mimic_lite/tasks/actions.py` | 3 | `JointPosition(residual=true)` |
| `projects/mimic-lite/mimic_lite/tasks/rewards/common.py` | 3 | 补 `residual_l2` |
| `projects/mimic-lite/mimic_lite/assets/adam_lite/__init__.py` | 1/4 | ankleRoll Kp 改为 1.0，避开 mjlab 除零 |
| `projects/mimic-lite/scripts/train.py` | 5 | CUDA 后禁止 FK DataLoader fork |
| `projects/mimic-lite/mimic_lite/tasks/rewards/feet.py` | 落地 | 补 `feet_force_excess` |
| `projects/mimic-lite/cfg/task/tracking-adam_lite-jump.yaml` | 落地/存活 | 接触力惩罚 + 存活/跟踪权重 |
| `projects/mimic-lite/cfg/task/termination/adam_lite_jump.yaml` | 存活 | `root_pos_error` 0.4→0.55 |
| `projects/mimic-lite/scripts/eval_adam_lite_jump.py` | 落地 | 当前步总 GRF，不再对历史 amax |

---

## 风险与阶段对应

| 风险 | 概率 | 影响 | 应对落在哪一阶段 |
|------|------|------|------------------|
| 数据集格式转换困难 | 中 | 中 | 阶段 2：按 any4hdmi 写转换脚本，并用 FK 回放验收 |
| 跳跃运动收敛慢 | 中 | 中 | 阶段 5 先小规模验证，阶段 6 再用 PPO-ROA |
| 落地冲击导致训练不稳定 | 高 | 中 | 阶段 4 开环暴露问题，阶段 3/6 再调终止和接触奖励 |
| 仿真到真实迁移差距大 | 中 | 中 | 阶段 6 finetune 加强域随机，阶段 7 再对比 deploy |

---

## 下一步

H1（电机 PD + `0.0025×8` + 0.85 扭矩）已过闸门，未倒地。下一步若继续往真机靠，跑 `--mode realish`（去掉训练 armature、Euler），不要开训。
