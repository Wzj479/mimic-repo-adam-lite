# 基于MimicLite策略的PND Adam Lite跳跃训练技术方案 v1.0

## 一、项目概述
### 1.1 项目目标
使用MimicLite框架的PPO/PPO-ROA运动跟踪策略，训练PND Adam Lite机器人实现双脚跳跃能力。

### 1.2 核心原则
- **资源划分**：仅使用`pnd_jump`仓库的**机器人模型**和**跳跃运动数据集**
- **训练方法**：完全采用MimicLite框架自带的训练策略，不使用`pnd_jump`的任何训练代码
- **模型兼容**：直接使用原生12DOF Adam Lite模型，无需关节映射

---

## 二、核心资源确认
### 2.1 从`pnd_jump`获取的资源
| 资源 | 路径/说明 | 用途 |
|------|-----------|------|
| **机器人模型** | `resources/robots/adam_lite/adam_lite_12dof.xml` | 12DOF腿部模型（跳跃专用，无腰/臂） |
| **跳跃运动数据** | `adam_lite_dataset/adam_lite/sfu/0005_2FeetJump001.bvh.json` | BVH格式跳跃轨迹（双脚跳） |
| **预训练策略** | `deploy/pre_train/adam_lite_12dof/confirm.pt` | 可选：作为教师策略参考 |

### 2.2 MimicLite训练方法
- **PPO单阶段训练**：`projects/mimic-lite/scripts/train.py`
- **PPO-ROA三阶段训练**：`projects/mimic-lite/scripts/train_sequential.py`（`train → adapt → finetune`）
- **运动跟踪框架**：MimicLite原生支持BVH/JSON格式运动数据集

---

## 三、技术方案
### 3.1 机器人模型集成（无需关节映射）
由于直接使用`pnd_jump`原生12DOF Adam Lite模型，关节定义完全保留：
- **12个驱动关节**：每条腿6个（hipPitch, hipRoll, hipYaw, kneePitch, anklePitch, ankleRoll）
- **浮动基座**：pelvis自由关节
- **默认站立姿态**：`[-0.32, 0.0, -0.18, 0.66, -0.29, 0.0, -0.32, 0.0, 0.18, 0.66, -0.29, 0.0]`

**实施步骤**：
1. 复制模型文件到`projects/mimic-lite/mimic_lite/assets/adam_lite/`
2. 创建资产定义`projects/mimic-lite/mimic_lite/assets/adam_lite.py`，直接加载原生MJCF
3. 注册到MimicLite资产系统

### 3.2 跳跃运动数据集适配
**数据集格式**：BVH转换为JSON格式，包含：
- 关节位置序列
- 根位置/旋转序列
- 足端接触状态
- 运动相位信息

**实施步骤**：
1. 将`0005_2FeetJump001.bvh.json`转换为MimicLite支持的`any4hdmi`格式
2. 创建运动配置`projects/mimic-lite/cfg/task/motion/adam_lite/jump.yaml`
3. 配置RSI（Reference State Initialization）参数，与`pnd_jump`一致

### 3.3 任务配置设计
基于`pnd_jump`验证的奖励结构，设计MimicLite跳跃跟踪任务：

```yaml
# projects/mimic-lite/cfg/task/tracking-adam_lite-jump.yaml
name: adam_lite_jump_tracking
robot: adam_lite_12dof

command:
  _target_: MotionTracking
  motion_config: adam_lite/jump
  rsi: true
  rsi_margin_frames: 50
  future_frame_offsets: [5, 10]  # 与pnd_jump一致

observation:
  joint_pos: {noise_std: 0.01}
  joint_vel: {noise_std: 0.05}
  root_pos: {}
  root_rot: {}
  foot_contacts: {}
  future_ref: {offsets: [5, 10]}  # 未来参考轨迹

reward:
  # 核心跟踪奖励（来自pnd_jump验证权重）
  dof_pos_tracking: {weight: 2.5, sigma: 0.25}
  root_pos_tracking: {weight: 3.5, sigma: 0.30}
  root_height_tracking: {weight: 2.5, sigma: 0.12}
  root_yaw_tracking: {weight: 2.0, sigma: 0.20}
  dof_vel_tracking: {weight: 1.2, sigma: 4.0}
  root_vel_tracking: {weight: 1.2}
  
  # 正则化奖励
  orientation: {weight: 1.0}
  foot_slip: {weight: -0.4}
  action_rate: {weight: -0.01}
  dof_acc: {weight: -2.5e-7}
  alive: {weight: 0.15}
  
  # 残差动作惩罚（与pnd_jump residual control匹配）
  residual_l2: {weight: -0.05}

termination:
  max_episode_length: 22000  # 22秒 @ 1kHz
  crash: {body_names: ["pelvis", "shin", "thigh"]}
  fall_over: {pitch_thres: 0.95, roll_thres: 0.85}
  root_z_below_ref: {thres: 0.35}

control:
  mode: residual  # q_des = q_ref + action * action_scale
  action_scale: 0.35
  stiffness: {hipPitch: 305, hipRoll: 700, hipYaw: 405, kneePitch: 305, anklePitch: 25, ankleRoll: 0}
  damping: {hipPitch: 6.1, hipRoll: 30, hipYaw: 6.1, kneePitch: 6.1, anklePitch: 3, ankleRoll: 0.35}
```

### 3.4 域随机化配置
```yaml
randomization:
  friction: {range: [0.4, 1.6]}
  base_mass: {range: [-0.05, 0.05]}
  push_robots: false  # 基础训练关闭，finetune阶段开启
  action_delay: {steps: 1}
```

---

## 四、训练流程
### 4.1 方案A：PPO单阶段训练（快速验证）
```bash
# 单GPU验证
uv --project venv/mjlab run \
  projects/mimic-lite/scripts/train.py \
  task=tracking-adam_lite-jump \
  algo/ppo/module=base \
  task.num_envs=1024 \
  total_frames=10_000_000

# 多GPU全量训练
bash scripts/launch_ddp.sh 0,1,2,3,4,5,6,7 projects/mimic-lite/scripts/train.py venv/mjlab \
  task=tracking-adam_lite-jump \
  algo/ppo/module=huge \
  task.num_envs=16384 \
  total_frames=100_000_000
```

### 4.2 方案B：PPO-ROA三阶段训练（推荐）
```bash
# 阶段1：基础运动跟踪能力
uv --project venv/mjlab run \
  projects/mimic-lite/scripts/train_sequential.py \
  task=tracking-adam_lite-jump \
  stage=train \
  algo/ppo_roa/module=huge \
  task.num_envs=8192

# 阶段2：跳跃运动适配（启用更强的跟踪奖励）
uv --project venv/mjlab run \
  projects/mimic-lite/scripts/train_sequential.py \
  task=tracking-adam_lite-jump \
  stage=adapt \
  checkpoint_path=${stage1_checkpoint} \
  algo/ppo_roa/module=huge

# 阶段3：跳跃性能微调（启用域随机化）
uv --project venv/mjlab run \
  projects/mimic-lite/scripts/train_sequential.py \
  task=tracking-adam_lite-jump \
  stage=finetune \
  checkpoint_path=${stage2_checkpoint} \
  algo/ppo_roa/module=huge \
  task.randomization.push_robots=true
```

---

## 五、代码修改清单
### 5.1 新增文件
| 文件路径 | 说明 |
|----------|------|
| `projects/mimic-lite/mimic_lite/assets/adam_lite.py` | 12DOF Adam Lite资产定义 |
| `projects/mimic-lite/mimic_lite/assets/adam_lite/adam_lite_12dof.xml` | 机器人MJCF模型 |
| `projects/mimic-lite/mimic_lite/assets/adam_lite/assets/*.obj` | 机器人网格文件 |
| `projects/mimic-lite/cfg/task/motion/adam_lite/jump.yaml` | 跳跃运动配置 |
| `projects/mimic-lite/cfg/task/tracking-adam_lite-jump.yaml` | 跳跃跟踪任务配置 |
| `scripts/convert_bvh_to_mimiclite.py` | BVH转MimicLite数据集格式脚本 |

### 5.2 修改文件
| 文件路径 | 修改内容 |
|----------|----------|
| `projects/mimic-lite/mimic_lite/assets/__init__.py` | 注册AdamLite资产 |
| `projects/mimic-lite/mimic_lite/tasks/command.py` | 适配跳跃运动RSI配置 |

---

## 六、关键优势
1. **无需关节映射**：直接使用原生12DOF Adam Lite模型，保留原始关节定义
2. **奖励函数已验证**：基于`pnd_jump`已验证的奖励权重，无需重新调参
3. **控制模式一致**：残差控制`q_des = q_ref + action * 0.35`与`pnd_jump`完全一致
4. **观测空间对齐**：82维观测（含未来5/10帧参考）与`pnd_jump` v4版本一致

---

## 七、预期时间线
| 阶段 | 时间 | 交付物 |
|------|------|--------|
| 机器人模型集成 | 0.5天 | 可加载的12DOF Adam Lite环境 |
| 数据集转换 | 0.5天 | MimicLite格式的跳跃数据集 |
| 任务配置 | 0.5天 | 可运行的跳跃测试环境 |
| 训练执行 | 2-3天 | 收敛的跳跃策略 |
| 评估导出 | 0.5天 | ONNX部署模型 |

---

## 八、风险与应对
| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| 数据集格式转换困难 | 中 | 中 | 参考`any4hdmi`数据集工具，编写适配转换脚本 |
| 跳跃运动收敛慢 | 中 | 中 | 使用PPO-ROA三阶段训练，先学习基础再适配跳跃 |
| 落地冲击导致训练不稳定 | 高 | 中 | 添加足端接触奖励和落地冲击惩罚，调整终止条件 |
| 仿真到真实迁移差距大 | 中 | 中 | 增加域随机化强度，添加执行器模型 |

---

## 九、评估指标
| 指标 | 说明 | 目标值 |
|------|------|--------|
| 跳跃高度误差 | 实际高度 vs 目标高度 | < 0.05m |
| 起跳速度峰值 | 垂直方向最大速度 | > 2.0 m/s |
| 腾空时间 | 双脚离地持续时间 | > 0.3s |
| 落地冲击力 | 落地时足端力峰值 | < 500N |
| 跳跃成功率 | 成功完成跳跃的比例 | > 95% |
| 能量效率 | 单位高度消耗的能量 | < 50 J/cm |

---

## 十、参考资源
1. **pnd_jump仓库**：https://github.com/Wzj479/pnd_jump.git
2. **MimicLite项目**：`projects/mimic-lite/`
3. **any4hdmi数据集格式**：https://huggingface.co/collections/elijahgalahad/any4hdmi
4. **MimicLite训练文档**：`projects/mimic-lite/README.md`

---

*文档版本：v1.0*  
*创建日期：2026-09-07*  
*作者：高级机器人运动控制工程师*
