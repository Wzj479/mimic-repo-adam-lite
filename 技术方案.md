# PND Adam Lite 双脚跳跃技术方案（当前）

*相对 `技术方案v1.0.md`：v1.0 是开训前规划；本文是 **当前有效方案**，并作为技术框架 / 核心算法说明。阶段闸门与改动清单见 `修改实践.md`。*  
*日期：2026-09-09*

## 当前状态（一句话）

**MimicLite PPO-ROA 已训出可用跳跃策略（`checkpoint_1000`）。** mjlab 评估过闸门。原生 MuJoCo 在对齐训练物理后可跑完整段 clip。冻结权重 + 部署踝 PD 为交付默认。硬 sim2sim 的 H1（电机 PD）已过闸门。下一步是 `--mode realish`，默认不训练。

---

## 1. 目标、原则与要学的东西

**目标**：用 MimicLite 的运动跟踪 RL，训练 **PND Adam Lite 原生 12DOF** 完成双脚跳跃。

本项目的技术问题可以收成一句：**有一条示范跳跃轨迹（位置，不是力矩）；把它变成 50 Hz 可查询的参考运动；每个并行环境把「机器人真实状态」和「参考当前/短未来」融合成观测；PPO-ROA 先让能看见特权信息的教师学会跟踪，再让只能看见部署观测的学生模仿教师的隐变量，最后只用学生做 PPO。**

| 项 | 约定 |
|----|------|
| 借用 | 只借 `pnd_jump` 的 **机器人 XML** 和 **跳跃运动数据** |
| 训练 | 只用 MimicLite（`active-adaptation/projects/mimic-lite/`），不迁 `pnd_jump` 训练代码 |
| 机器人 | 原生 `adam_lite_12dof.xml`，不做 G1 替换、不做 G1→Adam 关节映射 |
| 控制 | 残差 `q_des = q_ref + action * 0.35`（环境层 PD，不是网络直接出扭矩） |
| 观测 | MimicLite 原生学生观测（**不是** pnd 的 82 维 LSTM） |
| 奖励 | MimicLite 奖励骨架 + pnd_jump 权重映射 |

**明确不做**：把 MimicLite ONNX 塞进 `pnd_jump` 的 `build_jump_obs`；用走路部署的 47 维观测跑跳跃。

公式按数据流排在后面，可按节跳读：

| 节 | 推什么 |
|----|--------|
| 4.2 | 120→50 Hz 插值、SLERP、\(\dot{q}\) 差分、FK |
| 5.1 | 动作延迟、残差 \(q^{\mathrm{des}}\)、PD 力矩 |
| 5.2 | yaw 局部系、command/policy 维数、VecNorm、RSI |
| 5.3 | 跟踪误差、指数核奖励、终止 |
| 6.1 | 对角高斯 \(\log\pi\)、熵、KL |
| 6.3 | TD 残差 → GAE → 标准化优势 |
| 6.4 | 重要性采样 → PPO clip、价值 MSE、熵与 \(z\) 正则 |
| 6.5 | 学生隐变量蒸馏 \(L^{\mathrm{priv}}\) |

---

## 2. 技术框架总览

数据与算法分成六次格式转换。② 是位置轨迹，不是力矩；③ 是运动学参考表，不是物理仿真滚出来的；⑤ 才是 RL。

```
① BVH JSON（120 Hz，关节/根位置）
    → convert_bvh_to_mimiclite.py
② MuJoCo qpos[T, 32]（构型，无扭矩）
    → any4hdmi 插值 + 差分 qvel + FK
③ GPU 上的参考运动表（50 Hz，可按 t 查询）
    → MimicLite RobotTracking 融合仿真状态
④ MDP：command / policy / priv / 残差 PD / 奖励
    → PPO-ROA 三阶段
⑤ checkpoint（教师 + 学生 + VecNorm）
    → 只导出学生 + 冻结 VecNorm
⑥ ONNX（command=168 + policy=246）→ 原生 MuJoCo sim2sim
```

| 层 | 组件 | 作用 |
|----|------|------|
| 源数据 | Hugging Face `adam_lite_dataset` 的 `0005_2FeetJump001.bvh.json` | 示范跳跃，字段是位置/姿态 |
| 机器人 | `adam_lite_12dof.xml` | `nq=32`：7 维浮动基座 + 12 腿 + 锁死腰臂 |
| 运动库 | any4hdmi | 把 qpos 变成可 `get_slice` 的参考表 |
| 任务 | Hydra `task=tracking-adam_lite-jump` | 观测、奖励、终止、RSI |
| 仿真 | mjlab / MuJoCo，`dt=0.005×4` → **50 Hz** | 4096 个环境并行 |
| 算法 | MimicLite PPO-ROA（`huge`） | train → adapt → finetune |
| 导出 | 学生 ONNX | 部署不再加载 any4hdmi |

训练评估仍走 ③④⑤；sim2sim **不再用** GPU 运动表，而是用同一套 `future_steps` 从 JSON 临时拼 `command`。

---

## 3. 数据来源

只有两类外部数据，都不含训练代码、不含力矩标签。

### 3.1 机器人模型

路径：`mimic_lite/assets/adam_lite/adam_lite_12dof.xml`（源自 pnd）。

- 12 个驱动关节：每腿 `hipPitch / hipRoll / hipYaw / kneePitch / anklePitch / ankleRoll`
- 浮动基座：`pelvis`（free joint，qpos 前 7 维）
- 腰/臂：MJCF equality 锁死（肘 `-1.6`，其余 `0`），策略不管
- 默认站立：`[-0.32, 0, -0.18, 0.66, -0.29, 0, -0.32, 0, 0.18, 0.66, -0.29, 0]`

训练 XML 去掉视觉 mesh，只留碰撞体。`qpos=32` 是完整仿真状态；策略动作只有 **12** 维腿关节。

### 3.2 示范跳跃（位置轨迹，不是力矩）

源：Hugging Face `pndbotics/adam_lite_dataset` → 本仓库 `adam_lite_dataset/adam_lite/sfu/0005_2FeetJump001.bvh.json`。GitHub `pnd_jump` 不含这份数据。

| 项 | 值 |
|----|----|
| 帧率 | 120 Hz（`FrameDuration ≈ 1/120`） |
| 长度 | 2565 帧，约 21.4 s |
| 根位置 | `root_pos/{x,y,z}` |
| 根姿态 | `root_quat` **xyzw**（写入 MuJoCo 时转 **wxyz**） |
| 关节 | `dof_pos/<关节名>`，单位是角度，不是 τ |

这是动捕/重定向后的 **构型序列**。没有接触力、没有 PD 目标、没有「这一帧该用多大扭矩」。开环把策略动作置零，只靠 PD 追 `q_ref`，平均只活约 1.1 s，说明示范轨迹比弱踝 PD 跟得上的更猛，必须靠 RL 补残差。

---

## 4. 数据处理：从 JSON 到可查询参考运动

对应总览的 ①→②→③。全程不出现力矩。

### 4.1 ② 把 JSON 填进机器人 qpos

脚本：`projects/mimic-lite/scripts/convert_bvh_to_mimiclite.py`。

输出（any4hdmi 目录约定）：

```
any4hdmi/output/adam_lite/jump/
  manifest.json                 # timestep=1/120，qpos_dim=32，qpos_names，MJCF 路径
  motions/0005_2FeetJump001.npz # 唯一数组 qpos[T, 32]
```

`qpos` 每一帧：

| 段 | 内容 |
|----|------|
| `[0:3]` | 骨盆位置 |
| `[3:7]` | 骨盆四元数 wxyz |
| 铰链 | 12 腿关节角 + 锁死腰臂角 |

转换时 **保持 120 Hz，不在这里重采样**。随后用 MuJoCo FK 验收：骨盆相对抬升约 +0.29 m，最长双脚离地约 0.44 s。通过才说明通道和四元数没填反。

### 4.2 ③ 120 Hz → 50 Hz，再正运动学展开

训练时 `RobotTracking` 调用 any4hdmi，`f_{\mathrm{tgt}} = 1/\Delta t_{\mathrm{ctrl}} = 50\,\mathrm{Hz}`。源帧率 \(f_{\mathrm{src}}=120\)。全程不出现力矩。

**（1）重采样长度**

源序列 \(T_{\mathrm{src}}=2565\)。时长与目标帧数：

\[
T_{\mathrm{sec}} = \frac{T_{\mathrm{src}}-1}{f_{\mathrm{src}}},\qquad
T_{\mathrm{tgt}} = \lfloor T_{\mathrm{sec}}\, f_{\mathrm{tgt}} \rfloor + 1 \approx 1069
\]

目标时刻 \(t'_k = k / f_{\mathrm{tgt}}\)，\(k=0,\ldots,T_{\mathrm{tgt}}-1\)。源时刻 \(t_j = j / f_{\mathrm{src}}\)。

**（2）位置线性插值，四元数 SLERP**

对标量 qpos（平移、铰链角），在相邻源帧 \(t_L \le t' \le t_R\) 上：

\[
\alpha = \frac{t'-t_L}{t_R-t_L},\qquad
q(t') = (1-\alpha)\, q(t_L) + \alpha\, q(t_R)
\]

对根四元数（以及 FK 后的 `body_quat`）用球面插值。先把 \(q_R\) 翻到与 \(q_L\) 同半球（\(\langle q_L,q_R\rangle < 0\) 则 \(q_R \leftarrow -q_R\)），\(\theta_0=\arccos(\langle q_L,q_R\rangle)\)：

\[
\mathrm{slerp}(q_L,q_R;\alpha)
=
\frac{\sin((1-\alpha)\theta_0)}{\sin\theta_0}\, q_L
+
\frac{\sin(\alpha\theta_0)}{\sin\theta_0}\, q_R
\]

\(\theta_0\) 过小时退化为归一化线性插值（NLERP）。JSON 的 xyzw 在写入 qpos 时已换成 MuJoCo 的 wxyz：

\[
q_{\mathrm{wxyz}} = (w,x,y,z) \leftarrow (q_w, q_x, q_y, q_z)_{\mathrm{xyzw}}
\]

**（3）速度由位置差分，不是力矩、也不是传感器**

在 50 Hz 网格上，铰链 / 滑动自由度（\(\Delta t = 1/50\)）：

\[
\dot{q}_t = \frac{q_{t+1}-q_t}{\Delta t} = 50\,(q_{t+1}-q_t)
\]

浮动基座线速度同样差分。角速度由相邻四元数的相对旋转得到。记 \(q_t^{-1}\) 为共轭（单位四元数），相对旋转 \(q_{\Delta}=q_t^{-1} q_{t+1}=(w_{\Delta},\,\mathbf{v}_{\Delta})\)：

\[
\phi = 2\,\mathrm{atan2}(\|\mathbf{v}_{\Delta}\|, w_{\Delta}),\qquad
\boldsymbol{\omega}_t = \frac{\phi}{\Delta t}\,\frac{\mathbf{v}_{\Delta}}{\|\mathbf{v}_{\Delta}\|}
\]

\(\phi\) 折到 \((-\pi,\pi]\)。最后一帧复制前一帧速度。

**（4）FK**

把 \((q,\dot{q})\) 送进同一份 MJCF 的正向运动学（不算接触、不 `mj_step`）：

\[
\bigl(p^b_w,\, R^b_w,\, v^b_w,\, \omega^b_w\bigr)
=
\mathrm{FK}(q,\dot{q})
\]

得到每条跟踪连杆在世界系的位置、姿态、线速度、角速度，以及跟踪关节的 \(q_j,\dot{q}_j\)。③ 因此是示范在机器人模型上的 **运动学重放**；落地冲击不在表里。

本任务跟踪连杆：`pelvis`、`torso`、`thigh*`、`shin*`、`toe*`。工程上 CUDA 初始化后再 DataLoader fork 建 FK cache 会卡死，训练脚本固定 `ANY4HDMI_CACHE_BUILD_NUM_WORKERS=0`。

### 4.3 GPU 上的表：按时间查询参考帧

`full_motion: true` 把整段 \(T_{\mathrm{tgt}}\) 帧放进 GPU，成为一份 **共享表** \(\mathcal{M}\)。环境 \(i\) 只持有指针 \(t_i\)。查询

\[
\mathcal{M}\bigl[t_i + h\bigr],\quad h\in\{-8,-4,-2,0,1,2,3,4\}
\]

越界则 clamp 到 clip 头尾。取出的是 **参考** 的过去 / 当前 / 短未来，不是仿真真实状态。

学生最多看 \(h=+4\)（80 ms）。`full_motion` 只决定表有多长：整段约 21 s 都能播；windowed 的 512 帧窗口只有约 10.2 s，会截断跳跃。部署不用这张表，按同一套 \(h\) 从 JSON 现算。

---

## 5. 数据融合：参考轨迹如何进入控制与网络

③ 是参考表 \(\mathcal{M}\)；仿真给出真实状态 \(s\)。融合发生在 MDP，不是网络自己预测整段轨迹。

记控制周期 \(\Delta t=0.02\,\mathrm{s}\)，每个控制步内物理子步 \(n_{\mathrm{dec}}=4\)、\(\Delta t_{\mathrm{sim}}=0.005\,\mathrm{s}\)。`future_steps` 必须含 \(0\)（观测当前参考）和 \(+1\)（奖励与 PD 的跟踪目标）。因此 **PD 追的是下一帧参考** \(q^{\mathrm{ref}}_{t+1}\)。

### 5.1 残差控制与 PD

环境层 `JointPosition(residual=true)` 与 PPO 的 `residual_action`（默认 false）不是一回事：网络输出残差 \(a\in\mathbb{R}^{12}\)，不在 actor 里再加 \(q^{\mathrm{ref}}\)。

**（1）动作延迟**

缓冲 \(a^{(0)}_t=a_t\)，\(a^{(k)}_t=a_{t-k}\)。`min_delay=max_delay=1`，控制步内第 \(s=0,\ldots,3\) 个物理子步取延迟档

\[
d = \left\lfloor \frac{1-s+n_{\mathrm{dec}}-1}{n_{\mathrm{dec}}} \right\rfloor
\]

即整个控制步都用上一拍动作。`alpha_range=[1,1]`，平滑系数 \(\alpha=1\)，无额外低通：\(a^{\mathrm{app}}=\tilde{a}\)。

**（2）残差位置目标**

缩放 \(s_a=0.35\)，关节偏置 \(\delta\) 来自域随机：

\[
q^{\mathrm{des}}_t = q^{\mathrm{ref}}_{t+1} + \delta + s_a\, a^{\mathrm{app}}_t
\]

开环 \(a=0\) 时 \(q^{\mathrm{des}}=q^{\mathrm{ref}}+\delta\)，纯 PD 跟踪。

**（3）位置执行器（训练 H0）**

\[
\tau = K_p \bigl(q^{\mathrm{des}} - q\bigr) - K_d \dot{q},\qquad
\tau \leftarrow \mathrm{clip}(\tau,\, -\tau_{\max},\, \tau_{\max})
\]

训练增益：髋/膝与 pnd 相同；`anklePitch` \(K_p=25,K_d=3\)；`ankleRoll` \(K_p=1.0\)（原 0 会在 mjlab 除零）。这是力矩第一次出现的地方，由 PD 算出，不在示范数据里。

### 5.2 坐标系、观测与 VecNorm

**（1）参考 yaw 局部系（学生 `command` 的根位置）**

把锚点姿态投影到水平 yaw。水平朝向 \(h=R(q^{\mathrm{anc}})[1,0,0]\) 的 xy 分量：

\[
\psi = \mathrm{atan2}(h_y, h_x),\qquad
q_\psi = \bigl(\cos(\psi/2),\, 0,\, 0,\, \sin(\psi/2)\bigr)
\]

锚点 xy 归零后，连杆位置变到 yaw 局部系：

\[
p^{\mathrm{loc}} = R(q_\psi)^\top \bigl(p_w - [p^{\mathrm{anc}}_x,\, p^{\mathrm{anc}}_y,\, 0]^\top\bigr)
\]

姿态 6D：相对旋转矩阵 \(R\) 的前两行（6 个数），避免直接喂四元数。

**（2）学生 `command`（168 维）——查表，不是预测**

偏移集合 \(H=\{-8,-4,-2,0,1,2,3,4\}\)。每一档拼接根局部位置 3 + 姿态 6D 6 + 12 关节角：

\[
c_t = \bigoplus_{h\in H}
\Bigl[
p^{\mathrm{loc}}_{\mathrm{pelvis}}(t+h),\;
R_{1:2,:}(t+h),\;
q^{\mathrm{ref}}_j(t+h)
\Bigr]
\in\mathbb{R}^{8\times 21}=\mathbb{R}^{168}
\]

**（3）学生 `policy`（246 维）——仿真历史**

投影重力 \(g_b = R(q_{\mathrm{root}})^\top [0,0,-1]^\top\)。历史档 \(K=\{0,1,2,3,4,8,16\}\)：

\[
o^{\mathrm{hist}}_t = \bigoplus_{k\in K}
\bigl[\omega_{t-k},\; g_{b,t-k},\; q_{t-k},\; \dot{q}_{t-k}\bigr]
\in\mathbb{R}^{7\times 30}=\mathbb{R}^{210}
\]

再拼 3 步上一动作 \(a_{t-1:t-3}\)（36 维），得 \(o_t\in\mathbb{R}^{246}\)。

**（4）教师特权**

`command_long` 与 `command` 同类，patch 后 \(H_{\mathrm{long}}=\{-8,-4,-2,0,1,2,3,4,8,12,16\}\)（11 档）。`priv` 含：参考根在 **机器人本体** 系的位置/姿态、连杆空间误差、速度差、已施加动作与扭矩。教师能看见学生看不到的跟踪误差和更远未来。

**（5）VecNorm（EMA，`decay=0.9999`）**

对每路向量 \(x\) 维护加权和 \(S\)、平方和 \(Q\)、有效计数 \(n\)（\(\beta=0.9999\)，batch 大小 \(B\)）：

\[
n \leftarrow \beta n + B,\quad
S \leftarrow \beta S + \sum_{i=1}^{B} x_i,\quad
Q \leftarrow \beta Q + \sum_{i=1}^{B} x_i^{\odot 2}
\]

\[
\mu = \frac{S}{n},\qquad
\sigma = \sqrt{\max\bigl(Q/n - \mu^{\odot 2},\, \varepsilon\bigr)},\qquad
\hat{x} = \frac{x-\mu}{\sigma}
\]

\(\varepsilon=10^{-5}\)。`adapt` / `finetune` / 导出冻结 \(\mu,\sigma\)（打进 ONNX）。训练时观测还可加高斯噪声再 clamp 到 \(\pm 3\sigma_{\mathrm{noise}}\)。

**（6）RSI（相位平衡采样）**

clip 有效起点上界 \(T_{\max}=L-m-1\)，\(m=50\)。相位边 \((e_b,e_{b+1})\) 与归一化权重 \(w_b\) 定义桶 \(b\)：

\[
b\sim \mathrm{Categorical}(w),\qquad
t_0 \sim \mathrm{Unif}\{\lfloor e_b T_{\max}\rfloor,\, \lfloor e_{b+1} T_{\max}\rfloor\}
\]

终止时以概率 \(0.8\) rewind：\(t\leftarrow\max(t-U,0)\)，\(U\sim\mathrm{Unif}\{25,\ldots,125\}\)。评估 `start_from_zero` 则 \(t_0=1\)。

### 5.3 跟踪误差、奖励与终止

奖励 / PD 用的参考是缓冲里 **偏移 \(+1\)** 的那一帧。世界系与 yaw 局部系误差：

\[
\begin{aligned}
e^{p,b}_w &= \|p^{\mathrm{ref},b}_w - p^{b}_w\|_2,\\
e^{p,b}_{\mathrm{loc}} &= \|p^{\mathrm{ref},b}_{\mathrm{loc}} - p^{b}_{\mathrm{loc}}\|_2,\\
e^{R,b} &= \angle\bigl((q^{\mathrm{ref},b})^{-1} q^{b}\bigr),\\
e^{v,b} &= \|v^{\mathrm{ref},b}-v^{b}\|_2,\quad
e^{\omega,b} = \|\omega^{\mathrm{ref},b}-\omega^{b}\|_2,\\
e^{q}_j &= |q^{\mathrm{ref}}_j - q_j|,\quad
e^{\dot{q}}_j = |\dot{q}^{\mathrm{ref}}_j - \dot{q}_j|.
\end{aligned}
\]

指数核（YAML 的 `sigma`）：对选中的连杆/关节平均后

\[
r_{\exp} = \exp\bigl(-\bar{e}/\sigma\bigr) \in (0,1]
\]

环境写入的标量是 \(w\cdot r_{\mathrm{raw}}\)。跟踪项 \(r_{\mathrm{raw}}=r_{\exp}\)；多数 loco 惩罚的 \(r_{\mathrm{raw}}\) 已经带负号，YAML 权重为正。

| 项 | \(w\) | \(\sigma\) 或 \(r_{\mathrm{raw}}\) | 组 |
|----|------|------|----|
| `root_pos` | 3.5 | \(\sigma=0.30\)，骨盆 \(e^p_w\) | tracking |
| `root_ori` | 2.0 | \(\sigma=0.20\) | tracking |
| `root_linvel` | 1.5 | \(\sigma=1.0\) | tracking |
| `root_angvel` | 0.5 | \(\sigma=2.5\) | tracking |
| `body_pos` 局部 | 1.0 | \(\sigma=0.3\)，8 个跟踪连杆平均 | tracking |
| `body_ori` 局部 | 1.0 | \(\sigma=0.4\) | tracking |
| `joint_pos` | 2.5 | \(\sigma=0.25\)，12 关节 \(\lvert\Delta q\rvert\) 平均 | tracking |
| `joint_vel` | 1.2 | \(\sigma=4.0\) | tracking |
| `residual_l2` | 0.08 | \(-\|a^{\mathrm{app}}\|_2^2\) | loco |
| `action_rate_l2` | 0.02 | \(-\|a_t-a_{t-1}\|_2^2\) | loco |
| `joint_vel_l2` | \(5\cdot 10^{-4}\) | \(-\|\dot{q}\|_2^2\) | loco |
| `joint_pos_limits` | 10 | 超出软限位（0.95 倍行程）的量求和取负 | loco |
| `self_collisions` | -0.5 | \(\mathbf{1}[\exists\,\mathrm{力}>1\,\mathrm{N}]\) | loco |
| `feet_slip` | 0.4 | 接触时水平速度（clamp 到 1）求和取负 | loco |
| `feet_force_excess` | 0.6 | \(-\sum_f \max(F_f-500,0)/1000\) | loco |
| `survival` | 0.25 | \(1\) | loco |

组内求和得到 \(r^{\mathrm{trk}}_t,\, r^{\mathrm{loco}}_t\)。Critic 两头分别拟合这两组。500 N 只作观察：整机约 55 kg，站立总 GRF 已约 540 N。

**终止**（累计误差：连续超阈值满 `min_steps` 帧才触发）：

\[
\begin{aligned}
\mathrm{timeout}:&\quad t \ge L \;\text{或}\;
\max_b e^{p,b}_w \ge 0.55 \text{ 连续 } 25 \text{ 步（记 timeout）},\\
\mathrm{root\_z}:&\quad \max_b |z^{\mathrm{ref},b}-z^{b}| \ge 0.35,\\
\mathrm{fall}:&\quad \|g_{b,xy}\|_2 \ge 0.82,\\
\mathrm{ori}:&\quad \max_b e^{R,b} \ge 1.2 \text{ 连续 } 25 \text{ 步}.
\end{aligned}
\]

`term` 切断价值备份，`done`（含 timeout）切断 GAE 的 \(\lambda\) 链，见 §6.3。

---

## 6. PPO-ROA 公式推导

实现：`mimic_lite_learning/ppo_roa.py`。ROA = Regularized Online Adaptation。本任务 `huge`：隐变量 1024 维，encoder/actor 宽层 1024。优化器 Muon。收集长度 \(T_{\mathrm{roll}}=32\)，PPO 5 epoch × 8 minibatch。\(\gamma=0.99\)，\(\lambda=0.95\)，\(\epsilon=0.2\)。

### 6.1 策略是对角高斯，不是确定性网络

教师 / 学生 actor 输出均值 \(\mu\in\mathbb{R}^{12}\)；标准差 \(\sigma\in\mathbb{R}^{12}_{>0}\) 是 **与状态无关** 的可学习向量（`ActorROA.actor_std`，下限 \(10^{-6}\)）：

\[
\pi(a\mid s) = \prod_{j=1}^{12} \mathcal{N}(a_j;\, \mu_j(s),\, \sigma_j^2)
\]

密度的对数（PPO 的 \(\log\pi\)）：

\[
\log\pi(a\mid s)
=
-\frac12\sum_{j=1}^{12}
\Bigl[
\log(2\pi\sigma_j^2) + \Bigl(\frac{a_j-\mu_j}{\sigma_j}\Bigr)^2
\Bigr]
\]

熵（越大探索越强）：

\[
H[\pi] = \frac12 \sum_{j=1}^{12} \bigl(1 + \log(2\pi\sigma_j^2)\bigr)
\]

两高斯之间的 KL（用于调学习率，**不加进损失**）：

\[
\mathrm{KL}(\pi\|\pi_{\mathrm{old}})
=
\sum_j \Biggl[
\log\frac{\sigma^{\mathrm{old}}_j}{\sigma_j}
+ \frac{\sigma_j^2 + (\mu_j-\mu^{\mathrm{old}}_j)^2}{2(\sigma^{\mathrm{old}}_j)^2}
- \frac12
\Biggr]
\]

教师 encoder：\(z^{\mathrm{T}} = E_{\mathrm{T}}(\hat{o}_{\mathrm{priv}}, \hat{o}, \hat{c}_{\mathrm{long}})\)。  
学生 encoder：\(z^{\mathrm{S}} = E_{\mathrm{S}}(\hat{o}, \hat{c})\)。  
教师 actor 吃 \((\hat{o}, z^{\mathrm{T}})\)，学生 actor 吃 \((\hat{o}, z^{\mathrm{S}})\)。  
Critic \(V(s)\in\mathbb{R}^{2}\) 吃特权观测，分别估 tracking / loco。

`residual_action=false`：adapt **只对齐 \(z\)**，不对齐 \(\mu^{\mathrm{S}}\) 与 \(\mu^{\mathrm{T}}\)。

### 6.2 三阶段

| 阶段 | 迭代 | Rollout 策略 | 损失 | 更新 |
|------|------|----------------|------|------|
| train | 4000 | 教师 | PPO（教师）+ \(L^{\mathrm{reg}}\) + 辅助 \(L^{\mathrm{priv}}\) | 教师 encoder/actor、Critic；学生 encoder |
| adapt | 500 | 学生 | 仅 \(L^{\mathrm{priv}}\) | 仅学生 encoder |
| finetune | 1000 | 学生 | 仅学生 PPO（无 \(L^{\mathrm{reg}}\)），加推扰 | 学生 encoder/actor、Critic |

VecNorm 在 adapt/finetune 冻结。导出 ONNX = 冻结 VecNorm + \(E_{\mathrm{S}}\) + 学生 \(\mu\)（部署取均值，不采样）。

### 6.3 从回报到 GAE

目标是最大化折扣回报 \(G_t=\sum_{k=0}^{\infty}\gamma^k r_{t+k}\)。价值满足贝尔曼方程 \(V(s)=\mathbb{E}[r+\gamma V(s')\mid s]\)。一步 TD 残差：

\[
\delta_t = r_t + \gamma_t (1-\mathrm{term}_t)\, V(s_{t+1}) - V(s_t)
\]

其中 \(\gamma_t=\mathrm{discount}_t\cdot\gamma\)（接触等可乘在 `discount` 上）。\(n\) 步优势是 \(\delta_t+\gamma\lambda\delta_{t+1}+\cdots\)。TD(\(\lambda\)) 把所有 \(n\) 步指数加权，等价于后向递推（代码从 rollout 末尾倒着走）：

\[
A_t = \delta_t + \gamma_t\lambda\,(1-\mathrm{done}_t)\, A_{t+1},\qquad A_{T}=0
\]

\(\mathrm{term}\) 切断 \(V(s_{t+1})\)（摔倒不再备份）；\(\mathrm{done}\) 切断 \(\lambda\) 链（timeout 与 term 都算 episode 结束）。回报目标：

\[
R_t = A_t + V(s_t)
\]

本实现 `value_norm=false`，不对 \(R\) 再做 ValueNorm。两组奖励各算一套 \(A^{\mathrm{trk}},A^{\mathrm{loco}}\)。`normalize_before_sum=false`，先合成再标准化（`reward_scales` 对两组均匀 \(1/2\)）：

\[
A^{\mathrm{sum}}_t = \tfrac12 A^{\mathrm{trk}}_t + \tfrac12 A^{\mathrm{loco}}_t
\]

在非 `is_init` 的 mask 上：

\[
\hat{A}_t = \frac{A^{\mathrm{sum}}_t - \mathrm{mean}(A^{\mathrm{sum}})}{\mathrm{std}(A^{\mathrm{sum}})+10^{-5}}
\]

PPO 用的就是这个标量 \(\hat{A}_t\)。Reset 当步 `is_init=1`，不进平均。

### 6.4 从策略梯度到 PPO clip

策略目标 \(J(\theta)=\mathbb{E}_{\tau\sim\pi_\theta}[G_0]\)。score-function 梯度是 \(\mathbb{E}[\nabla\log\pi_\theta(a\mid s)\,\hat{A}]\)。用旧策略 \(\pi_{\mathrm{old}}\) 采样时要重要性采样：

\[
\rho_t(\theta) = \frac{\pi_\theta(a_t\mid s_t)}{\pi_{\mathrm{old}}(a_t\mid s_t)}
= \exp\bigl(\log\pi_\theta-\log\pi_{\mathrm{old}}\bigr)
\]

无约束 surrogate 是 \(\mathbb{E}[\rho\hat{A}]\)，\(\rho\) 过大一步会毁策略。PPO 把 \(\rho\) 限制在 \([1-\epsilon,1+\epsilon]\)，并取悲观的一边（\(\hat{A}>0\) 时不能把概率提太高，\(\hat{A}<0\) 时不能压太低）：

\[
L^{\mathrm{CLIP}}(\theta)
= -\mathbb{E}_t\Bigl[
\min\bigl(\rho_t\hat{A}_t,\; \mathrm{clip}(\rho_t,1-\epsilon,1+\epsilon)\hat{A}_t\bigr)
\Bigr]
\]

代码对 valid 步取平均。价值拟合：

\[
L^{V} = \frac{1}{|\mathrm{valid}|}\sum_{t\in\mathrm{valid}}
\sum_{g\in\{\mathrm{trk},\mathrm{loco}\}}
\bigl(V^g_\theta(s_t) - R^g_t\bigr)^2
\]

熵写成损失（\(c_{\mathrm{ent}}\) 越大越敢探索）：

\[
L^{S} = -c_{\mathrm{ent}}\, H[\pi_\theta]
\]

**train 阶段隐变量正则**（学生向教师对齐，\(c_{\mathrm{reg}}\) 从 0 线性升到 0.2，iter 500→1000）：

\[
L^{\mathrm{reg}} = \mathbb{E}\bigl[\|z^{\mathrm{S}}_t - z^{\mathrm{T}}_t\|_2^2\bigr]
\]

教师 PPO 总损失：

\[
L^{\mathrm{PPO}} = L^{\mathrm{CLIP}} + L^{S} + \overline{L^{V}} + c_{\mathrm{reg}} L^{\mathrm{reg}}
\]

`finetune` 把教师换成学生，且 \(c_{\mathrm{reg}}=0\)。

**熵系数与 KL 学习率**（相对 `total_iters` 的进度 \(u\in[0,1]\)；train 从 0.75 线性收到 1.0）：

\[
c_{\mathrm{ent}}(u) = 0.01 + (0.002-0.01)\,u,\qquad
\mathrm{KL}^{\star}(u)=0.01+(0.005-0.01)\,u
\]

若 minibatch KL 的 p90 \(> 2\cdot\mathrm{KL}^{\star}\)，则 \(\eta\leftarrow\eta/1.2\)；若均值 KL \(<\mathrm{KL}^{\star}/2\) 且尾部也不高，则 \(\eta\leftarrow\min(10^{-2},1.2\eta)\)。finetune 钉死 \(c_{\mathrm{ent}}=0.002\)，\(\eta=10^{-4}\)。

### 6.5 Adapt：蒸馏教师隐变量

教师 encoder 停止梯度。学生只看部署观测，要在隐空间复现教师：

\[
L^{\mathrm{priv}}
= \mathbb{E}_{t\notin\mathrm{init}}\Bigl\|
E_{\mathrm{S}}(\hat{o}_t,\hat{c}_t)
-
\mathrm{sg}\bigl(E_{\mathrm{T}}(\hat{o}^{\mathrm{priv}}_t,\hat{o}_t,\hat{c}^{\mathrm{long}}_t)\bigr)
\Bigr\|_2^2
\]

每个 collected batch 做 2 轮 × 8 minibatch。直观：教师已经用特权学会「现在该怎么动」；学生必须把同一意图压进 1024 维 \(z^{\mathrm{S}}\)，部署时没有 priv / 长未来也能用。

`train` 里 \(L^{\mathrm{priv}}\) 与 \(L^{\mathrm{reg}}\) 同类，但优化器不同：\(L^{\mathrm{reg}}\) 走教师 PPO 的 `opt_policy` 旁路统计学生输出；`opt_adapt` 才真正更新 \(E_{\mathrm{S}}\)。`adapt` 阶段关掉 PPO，只跑 \(L^{\mathrm{priv}}\)。

### 6.6 为何拆成 ROA 而不是一次 PPO

| | 普通 PPO（路径 A，未跑） | PPO-ROA（路径 B，已跑完） |
|--|--------------------------|---------------------------|
| 部署网络 | 直接在 168+246 上 PPO | 教师先看特权，再蒸馏 \(z\) |
| 跳跃 | 学生一开始看不到误差和远未来，难起跳 | 教师用 `command_long`+`priv` 先把跟踪做出来 |
| 本机 | 规划 8 卡 / 8192 env | 单卡 4096 env，约 4.3 h |

Rollout：`train` 用教师采样；之后及 ONNX 用学生。部署取 \(\mu\)，不再加 \(\sigma\) 噪声。

---

## 7. 训练结果与冻结权重

- Run：`active-adaptation/outputs/2026-09-08/12-21-37-train-sequential/`
- `01-train` 4000 → `02-adapt` 500 → `03-finetune` 1000（含推扰）
- 小规模单阶段 PPO（1024 env / 10M 帧）只用来证明能学、不 NaN，平均存活约 1.55 s，完整跳跃形态未出现

### 7.1 最佳：finetune `checkpoint_1000.pt`

mjlab 64 环境、`start_from_zero`：

| 指标 | 目标 | 实测 |
|------|------|------|
| 高度误差 | < 0.05 m | **0.032 m** |
| 起飞 vz | > 2.0 m/s | **2.67 m/s** |
| 腾空 | > 0.3 s | **0.45 s** |
| 成功率 | > 95% | **98.4%** |
| 平均存活 | clip ~21.4 s | 18.5 s |
| 峰值抬升 | ref ~0.29 m | 0.26 m |

### 7.2 不要当更好跳跃权重：落地 FT `checkpoint_1600`

接触力惩罚再 finetune 后高度误差 **0.057 m**（闸门失败），平均存活 16 s。跳高继续用 **ckpt_1000**。

### 7.3 产物

| 产物 | 路径 |
|------|------|
| 最佳 ckpt | `.../12-21-37-train-sequential/stages/03-finetune/checkpoint_1000.pt` |
| 最佳 ONNX | `.../exports/AdamLiteJumpTrack/policy-adam_lite_jump_best.onnx` |
| 摘要 | 仓库根目录 `adam_lite_jump_best.json` |
| 一键 sim2sim | `bash sim2sim_jump_best.sh`（默认 `--pd deploy`） |

解释器：`active-adaptation/venv/mjlab/.venv/bin/python`。无头 `MUJOCO_GL=disabled`；录视频用 `egl`。

---

## 8. 原生 MuJoCo sim2sim

脚本：`projects/mimic-lite/scripts/sim2sim_adam_lite_jump.py`。场景/PD 可参考 pnd，**观测必须是 MimicLite ONNX**。

早期 ~6 s 倒地是部署环与训练不一致（步长、执行器、armature、积分器、起始帧、历史写入、动作延迟），不是 ckpt_1000 坏了。对齐后可跑完整 clip ~21.34 s。

| `--mode` | 含义 | 推荐 |
|----------|------|------|
| `train`（H0） | position 执行器，`0.005×4`，满 effort，`implicitfast` | 对齐训练 |
| `motor`（H1） | 电机 + Python PD，`0.0025×8`，扭矩 ×0.85 | 已过闸门 |
| `realish`（H2） | 去训练 armature、Euler | 更硬，非当前默认 |

默认 `--pd deploy`（踝 Pitch 30/3、Roll 3/0.35）：跳高几乎不变，H0 峰值总 GRF 约 5348 N → **4697 N**。H1 同样未倒地，起飞约 2.51 m/s，腾空 0.44 s。

---

## 9. 与 v1.0 的差异

| v1.0 示意 | 当前事实 |
|-----------|----------|
| YAML 键名按规划稿 | Hydra：`RobotTracking`、any4hdmi、`tracking-base` 结构 |
| 观测对齐 pnd 82 维 future 5/10 | 学生 168+246，future `[-8…4]` |
| 运动数据「直接读 BVH」 | 先 qpos NPZ，再 FK 表；② 是位置不是力矩 |
| 落地 < 500 N 硬闸门 | 仅观察 |
| ankleRoll Kp=0 | 训练 Kp=1.0 |
| 直接丢进 pnd jump deploy | 必须自有 sim2sim |

实践清单仍见 `修改实践.md`。

---

## 10. 下一步（真机方向，默认不训练）

1. **H2** `bash sim2sim_jump_best.sh --mode realish`
2. **H3**：IMU 风格状态来源（不改策略维数则只改输入从哪读）
3. **H4**：独立跳跃 DDS，复用 LowCmd，不要走路 obs

若仍要跳高，不要从 `checkpoint_1600` 续训。
